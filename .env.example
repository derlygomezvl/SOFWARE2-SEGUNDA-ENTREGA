# ===========================================
# VARIABLES DE ENTORNO - GESTIÓN TRABAJO DE GRADO
# ===========================================
# Copiar este archivo como .env y configurar los valores reales

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-CHANGE-THIS-IN-PRODUCTION-minimum-256-bits-required-for-security

# RabbitMQ Configuration
RABBITMQ_USER=admin
RABBITMQ_PASS=admin_password_change_this

# Identity Service Database
IDENTITY_DB_USER=identity_user
IDENTITY_DB_PASS=identity_password_change_this

# Submission Service Database
SUBMISSION_DB_USER=submission_user
SUBMISSION_DB_PASS=submission_password_change_this

# Notification Service Database
NOTIFICATION_DB_USER=notification_user
NOTIFICATION_DB_PASS=notification_password_change_this

# SMTP Configuration (OPCIONAL - El sistema usa modo MOCK por defecto)
# Solo configurar si deseas enviar emails reales en producción
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@trabajogrado.com

# ===========================================
# NOTAS IMPORTANTES:
# ===========================================
# 1. NO subir el archivo .env a Git (ya está en .gitignore)
# 2. Cambiar todos los valores por defecto en producción
# 3. SMTP está en modo MOCK - los emails se registran en logs solamente
# 4. Para envío real de emails, cambiar NOTIFICATION_MAIL_MOCK=false en docker-compose
# 5. Para Gmail, usar "App Password" en lugar de contraseña normal
# 6. JWT_SECRET debe tener al menos 256 bits (32 caracteres)
